CREATE TABLE request_list (
                                        id SERIAL PRIMARY KEY,
                                        user_id BIGINT,
                                        friend_request_id BIGINT
)
